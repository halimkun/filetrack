version: '3.8'

services:
  app:
    image: filetrack:2024-06-21
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9000:3000"
    environment:
      NODE_ENV: production
    command: ["node", ".output/server/index.mjs"]
